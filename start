apk add nano openssh-client-krb5 krb5 tmux git
mv .profile ~
mv krb5.conf /etc/
mkdir ~/.ssh/
mv config ~/.ssh/
chmod +x lpccheck
mv lpccheck /usr/local/bin
cd ~
source .profile
rm -rf iSH-new-start-scripts
